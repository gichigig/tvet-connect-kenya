{
  "name": "tvet-secure-upload-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function for secure file uploads with Firebase Auth verification",
  "main": "index.js",
  "scripts": {
    "deploy": "zip -r function.zip . && aws lambda update-function-code --function-name tvet-secure-upload --zip-file fileb://function.zip",
    "package": "zip -r function.zip index.js node_modules/ package.json"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.478.0",
    "@aws-sdk/s3-presigned-post": "^3.478.0",
    "firebase-admin": "^11.11.1"
  },
  "keywords": ["aws", "lambda", "s3", "firebase", "secure-upload"],
  "author": "",
  "license": "MIT"
}
