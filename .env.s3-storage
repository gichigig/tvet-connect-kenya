# AWS S3 Configuration for Document Storage
# Add these environment variables to your .env file

# AWS S3 Configuration
VITE_S3_BUCKET_NAME=tvet-connect-documents
VITE_AWS_REGION=us-east-1
VITE_AWS_API_ENDPOINT=https://your-api-gateway-url.amazonaws.com/prod

# AWS Lambda API Gateway Endpoint for file operations
# This should point to your AWS Lambda function that handles:
# - Presigned POST URL generation
# - File upload coordination
# - File deletion
# - Download URL generation

# Backend AWS Configuration (for Lambda)
***REMOVED***=your-access-key-id
***REMOVED***=your-secret-access-key
AWS_REGION=us-east-1
S3_BUCKET_NAME=tvet-connect-documents

# Firebase Configuration (for metadata storage)
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_DATABASE_URL=https://your-project-default-rtdb.firebaseio.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id

# System Architecture:
# 1. Frontend (React/Vite) → AWS API Gateway → Lambda → S3
# 2. Document metadata stored in Firestore
# 3. File content stored in S3
# 4. Presigned URLs for secure access
